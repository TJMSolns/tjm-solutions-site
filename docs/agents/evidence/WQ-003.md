# Evidence — WQ-003

**Item:** Remove workshops card from homepage.

Commit: 68d9e90

Run-count: 1 (`npm run build` — passed; `npm run wcag` — pa11y WCAG2AA against homepage, no issues
found)

Invariance-recheck: Re-read `src/components/HomepageFeatures/index.tsx` post-commit and confirmed the
`FeatureList` array no longer contains a "Workshop Management" entry — exactly 3 entries remain
(Commerce Strategy, Digital Transformation, Experience Engineering). Confirmed the grid CSS
(`styles.module.css`) was updated to `repeat(3, 1fr)` for desktop so the 3 remaining cards render evenly
rather than leaving a dangling single card in a 2-column grid.

Verified-by: PENDING — verifier agent unavailable in this session (ESC-001)

Verifier-tier: N/A (ESC-001)

Verifier-verdict: PENDING (ESC-001 — see docs/agents/ESCALATIONS.md)
