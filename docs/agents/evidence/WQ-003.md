# Evidence — WQ-003

**Item:** Remove the workshops card from the homepage (leaving the three real category cards —
Commerce Strategy, Digital Transformation, Experience Engineering).

Commit: 68d9e90 (original removal; the homepage was later restyled by WQ-039/043, which did not
reintroduce workshops)

Run-count: 2 checks run at verification time (2026-07-22). (1) `npm run build` with
`onBrokenLinks: 'throw'` — passed. (2) Parsed `src/components/HomepageFeatures/index.tsx` — the
`FeatureList` array has exactly 3 entries (`code: 'CS' | 'DT' | 'EE'`) and `grep -ci workshop` on the
file returns 0.

Invariance-recheck: Re-read the current `src/components/HomepageFeatures/index.tsx` rather than trusting
the original post-commit note. Confirmed no "Workshop Management" (or any workshop) entry remains and
exactly the three category cards are present. **Note on a superseded detail:** the original WQ-003
evidence claimed the desktop grid was changed to `repeat(3, 1fr)`; that specific CSS was later replaced
by the WQ-039 site-wide redesign, which restyled the services index as a vertical stack of full-width
category rows (`.servicesIndex { flex-direction: column }` in `styles.module.css`). The redesign did not
reintroduce a workshops card, so WQ-003's actual deliverable — the workshops card is gone and only the
three real categories render — holds in current state. The layout mechanism differs from the original
note; the removal itself is intact. Independent verifier should confirm against current state: no
workshop entry in `index.tsx`, three category entries present.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model sonnet) — dispatched 2026-07-22.

Verifier-tier: sonnet (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=-1 final_offset=-1 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — awaiting the independent sonnet verifier's result.
