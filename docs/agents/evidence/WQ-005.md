# Evidence — WQ-005

**Item:** Enhance Commerce Strategy card: list the Commerce Strategy offers with links to their detail
pages. Original scope named the three June-selected offers (CS-2, CS-4, CS-3); **WQ-037 later expanded
every card to the full catalogue**, so the CS card now lists all 9 CS offers. Tony reviewed the cards
as they render today and signed off 2026-07-22 (HL-065).

Commit: 39b7862 (original wiring; superseded/expanded by WQ-037 commit 9dbf3f1)

Run-count: 2 checks run at verification time (2026-07-22). (1) `npm run build` with
`onBrokenLinks: 'throw'` — passed (`[SUCCESS] Generated static files`), which structurally proves every
CS-card href resolves to a real page (a typo'd slug would throw). (2) Parsed
`src/components/HomepageFeatures/index.tsx` — the CS `FeatureItem` carries exactly 9 `id: 'CS-N'` offer
entries, each with an `href` under `/services/`.

Invariance-recheck: Re-read the current `src/components/HomepageFeatures/index.tsx` rather than trusting
the original 3-offer claim (which WQ-037 superseded). The Commerce Strategy card's `offers` array now
lists all 9 CS offers — CS-1 through CS-9 — each mapping to its `/services/<slug>` detail page
(e.g. CS-2 → `/services/marketplace-integration-strategy`, CS-4 →
`/services/composable-commerce-architecture-review`, CS-3 → `/services/agent-commerce-readiness-audit`,
the three original June picks, now among the full set). The card renders these as linked chips via the
`Feature` component's `offerChips` map. WQ-005's deliverable — the CS category card wired to its offer
detail pages with working links — is satisfied by the current superset; the specific 3-offer list was a
starting selection, not a ceiling, and its expansion was Tony-reviewed (2026-07-22). Independent
verifier should re-check against the *current* file state (9 CS offers, all hrefs resolving), not the
original 3.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model opus) — dispatched 2026-07-22.

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=1 final_offset=1 resampled=False priority=P2 doer=opus)

Verifier-verdict: PASS — independent verifier (opus, fresh context, `subagent_type: "verifier"`, 4 tool
uses) confirmed both commits real (39b7862, 9dbf3f1), the current CS `FeatureItem` lists all 9 offers
CS-1..CS-9 each with a `/services/` href, the `Feature` component maps each to a Docusaurus `<Link>`
chip, and — via a filesystem check different from the doer's build — all 9 CS slugs exist as `.tsx`
pages in `src/pages/services/`, so every href resolves. No VETO issues.
