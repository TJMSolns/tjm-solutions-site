# Evidence — WQ-006

**Item:** Enhance Digital Transformation card: list the Digital Transformation offers with links to
their detail pages. Original scope named the three June-selected offers (DT-2, DT-3, DT-5); **WQ-037
later expanded every card to the full catalogue**, so the DT card now lists all 6 DT offers. Tony
reviewed the cards as they render today and signed off 2026-07-22 (HL-065).

Commit: 39b7862 (original wiring; superseded/expanded by WQ-037 commit 9dbf3f1)

Run-count: 2 checks run at verification time (2026-07-22). (1) `npm run build` with
`onBrokenLinks: 'throw'` — passed (`[SUCCESS] Generated static files`), structurally proving every
DT-card href resolves. (2) Parsed `src/components/HomepageFeatures/index.tsx` — the DT `FeatureItem`
carries exactly 6 `id: 'DT-N'` offer entries, each with an `href` under `/services/`.

Invariance-recheck: Re-read the current `src/components/HomepageFeatures/index.tsx` rather than trusting
the original 3-offer claim (which WQ-037 superseded). The Digital Transformation card's `offers` array
now lists all 6 DT offers — DT-1 through DT-6 — each mapping to its `/services/<slug>` detail page
(the three original June picks DT-2 → `/services/kcs-knowledge-base`, DT-3 →
`/services/microservices-modernization-assessment`, DT-5 → `/services/reactive-systems-reliability-review`
are among the full set). WQ-006's deliverable — the DT category card wired to its offer detail pages
with working links — is satisfied by the current superset; the 3-offer list was a starting selection,
its expansion Tony-reviewed (2026-07-22). Independent verifier should re-check against the *current*
file state (6 DT offers, all hrefs resolving), not the original 3. Note: DT-2's `/services/kcs-knowledge-base`
page is separately tracked for a PDR-008 credibility gap under WQ-016/WQ-045 — that is a content-policy
matter on the destination page, not a defect in this card's wiring, which is all WQ-006 covers.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model sonnet) — dispatched 2026-07-22.

Verifier-tier: sonnet (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=-1 final_offset=-1 resampled=False priority=P2 doer=opus)

Verifier-verdict: PASS — independent verifier (sonnet, fresh context, `subagent_type: "verifier"`, 4
tool uses) confirmed both commits real (39b7862 3-offer wiring, 9dbf3f1 expansion to 6), the current DT
`FeatureItem` lists exactly 6 offers DT-1..DT-6 each with a `/services/` href rendered as `<Link>` chips,
and — via a filesystem check different from the doer's build — all 6 DT slugs exist as `.tsx` pages in
`src/pages/services/`. No VETO issues.
