# Evidence — WQ-007

**Item:** Enhance Experience Engineering card: list the Experience Engineering offers with links to
their detail pages. Original scope named the three June-selected offers (EE-1, EE-5, EE-4); **WQ-037
later expanded every card to the full catalogue**, so the EE card now lists all 7 EE offers. Tony
reviewed the cards as they render today and signed off 2026-07-22 (HL-065).

Commit: 39b7862 (original wiring; superseded/expanded by WQ-037 commit 9dbf3f1)

Run-count: 2 checks run at verification time (2026-07-22). (1) `npm run build` with
`onBrokenLinks: 'throw'` — passed (`[SUCCESS] Generated static files`), structurally proving every
EE-card href resolves. (2) Parsed `src/components/HomepageFeatures/index.tsx` — the EE `FeatureItem`
carries exactly 7 `id: 'EE-N'` offer entries, each with an `href` under `/services/`.

Invariance-recheck: Re-read the current `src/components/HomepageFeatures/index.tsx` rather than trusting
the original 3-offer claim (which WQ-037 superseded). The Experience Engineering card's `offers` array
now lists all 7 EE offers — EE-1 through EE-7 — each mapping to its `/services/<slug>` detail page
(the three original June picks EE-1 → `/services/commerce-search-discovery-audit`, EE-5 →
`/services/agent-ready-experience-design`, EE-4 → `/services/engagement-architecture-review` are among
the full set). WQ-007's deliverable — the EE category card wired to its offer detail pages with working
links — is satisfied by the current superset; the 3-offer list was a starting selection, its expansion
Tony-reviewed (2026-07-22). Independent verifier should re-check against the *current* file state (7 EE
offers, all hrefs resolving), not the original 3.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model opus) — dispatched 2026-07-22.

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=0 final_offset=0 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — awaiting the independent opus verifier's result.
