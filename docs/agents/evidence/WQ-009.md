# Evidence — WQ-009

**Item:** Create individual offer detail pages — one per offer for the 9 offers selected under WQ-031 —
at `/services/<slug>` in `src/pages/services/`, each with problem statement, value prop, deliverables
table, duration, and pricing-deferred note. (Original scope also called for a per-page credibility
section; see the credibility note below.)

Commit: 090f720 (original 9 pages; WQ-036 later added the remaining 13 to complete all 22; WQ-042 later
removed the credibility sections site-wide)

Run-count: 2 checks run at verification time (2026-07-22). (1) `npm run build` with
`onBrokenLinks: 'throw'` — passed, confirming all `/services/*` routes and their internal links resolve.
(2) `ls src/pages/services/*.tsx | wc -l` = 22 — the 9 WQ-009 pages plus WQ-036's 13, all present as
real page files.

Invariance-recheck: Re-checked current filesystem state rather than trusting the original post-commit
note. All 9 WQ-009 slugs exist as `.tsx` pages under `src/pages/services/`
(agent-commerce-readiness-audit, agent-ready-experience-design, commerce-search-discovery-audit,
composable-commerce-architecture-review, engagement-architecture-review, kcs-knowledge-base,
marketplace-integration-strategy, microservices-modernization-assessment,
reactive-systems-reliability-review) and each is reachable via the homepage cards (verified in
WQ-005/006/007). **Credibility-section note (a deliberately superseded part of the original scope):**
WQ-009's original spec included a per-page credibility section per the WQ-031 map, and the original
evidence cross-checked those sections. **WQ-042 (commit 99feb28) subsequently removed all offer-page
credibility sections site-wide for legal-exposure reasons** — `grep -ril credibility src/pages/services/`
now returns 1 of 22. That removal is a **known, Tony-accepted state**: Tony reviewed the offer pages as
they render today and signed off WQ-009 on 2026-07-22, expressly decoupling the credibility question
("credibility question is separate, websites are for merchandising", HL-065). The unresolved
PDR-008-vs-WQ-042 policy reconciliation is tracked separately as WQ-045 and is **not** a defect in
WQ-009's page-creation deliverable. Independent verifier should confirm the 9 pages exist and render
with their core content (problem/value/deliverables/duration); the absence of credibility sections is
expected per the WQ-042 decision, not a VETO condition for WQ-009.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model opus) — dispatched 2026-07-22.

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=1 final_offset=1 resampled=False priority=P2 doer=opus)

Verifier-verdict: PASS — independent verifier (opus, fresh context, `subagent_type: "verifier"`, 10 tool
uses) confirmed commit 090f720 real (adds the shared `OfferPage` component + exactly the 9 named pages,
952 insertions), all 9 slugs present as real `.tsx` files (22 total with WQ-036's 13), opened 2 pages
and confirmed substantive `problemStatement`/`valueProposition`/`deliverables`/`duration`/`costBenefit`
content rendered via `OfferPage` (not stubs), `npm run build` (onBrokenLinks: throw) exit 0, and
independently re-derived the credibility carve-out (`grep -ril credibility src/pages/services/` = 1 of
22, the `credibility` field optional and unpopulated on the 9 pages) — expected per WQ-042 + Tony's
sign-off, not a VETO. **Honest nuance the verifier raised:** no literal "pricing-deferred note" sentence
exists (nor did it at the original commit), but pricing deferral is embodied on every page (no price
shown; each ends in a "Schedule a Consultation" CTA) and Tony reviewed/signed off the pages as they
render (HL-065). The four core content elements were all present and substantive. No VETO.
