# Evidence — WQ-017

**Item:** Add "Learn more → KCS Knowledge Base" link to Digital Transformation services card on
homepage; points to WQ-016 page; does NOT require ≥3 offers; does NOT close WQ-006.

**Groom finding (GL-030, 2026-07-10):** This item was never separately implemented under its own
commit either. Its substance — a working link from the homepage DT card to the WQ-016 (`kcs-knowledge-
base`) page — was delivered as part of WQ-006's card enhancement and carried forward through WQ-037's
chip redesign. This artifact records this pass's direct re-derivation of that claim.

Commit: 39b7862 (WQ-006: Digital Transformation card enhancement — original DT-2 link add); carried
through 9dbf3f1 (WQ-037: homepage offer list redesigned to chips, same href preserved)

Run-count: 2 (`grep -n "kcs-knowledge-base" src/components/HomepageFeatures/index.tsx` — one match,
`{id: 'DT-2', label: 'KCS Knowledge Base Implementation', href: '/services/kcs-knowledge-base'}`;
`git log -p --follow -- src/components/HomepageFeatures/index.tsx | grep -n "kcs-knowledge-base|^commit"`
— confirms the href was introduced in 39b7862 and still present, unchanged, as of the current HEAD)

Invariance-recheck: Read `src/components/HomepageFeatures/index.tsx` directly and confirmed the DT-2
entry renders inside the Digital Transformation category's `offers` array, which WQ-037's chip
component maps to a real `<a href=...>` per offer chip (same rendering path as the other 21 offers on
the homepage) — this is a live, resolvable link, not a stale reference. The literal microcopy "Learn
more → KCS Knowledge Base" specified in WQ-017's original description does not appear verbatim (the
chip UI uses a mono code + short label, not a sentence-style link, per WQ-037's Tony-directed redesign)
— the underlying requirement (a working homepage-to-WQ-016-page link, independent of the ≥3-offer gate,
not itself closing WQ-006) is met; the exact copy was superseded by a later, Tony-directed UI pattern
change, not left undone. Confirmed via the same file read that this does not require or claim closure
of WQ-006 (WQ-006 is tracked and closed separately, still gated on ESC-001).

Verified-by: verifier (Agent tool, subagent_type: "verifier")

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P4 sonnet` — raw_offset=1
final_offset=1 resampled=False)

Verifier-verdict: PASS — independent verifier (opus, fresh context, subagent_type "verifier",
dispatched as a background Agent call for DN-006 transcript corroboration) confirmed commit 39b7862 is
real and touches `src/components/HomepageFeatures/index.tsx`; independently read the current file and
confirmed the live `{id: 'DT-2', ..., href: '/services/kcs-knowledge-base'}` entry renders through
`offers.map(...)` into a real resolvable `<Link>` (same path as all other offers); independently ran
`git log -S`/`git grep HEAD` (a different check than the doer's `git log -p | grep`) confirming the href
was introduced in 39b7862 and is still present unchanged, preserved through WQ-037 (9dbf3f1); additionally
confirmed the target page exists in both source and build output (`src/pages/services/kcs-knowledge-base.tsx`,
`build/services/kcs-knowledge-base.html`) — a perturbation the doer's own check did not cover. Confirmed
the literal "Learn more →" microcopy is absent but superseded by Tony-directed WQ-037 chip redesign
(WORK-QUEUE line 84), and that WQ-006 is tracked/closed as its own separate row, not implicitly closed
by this item. No VETO issues found.
