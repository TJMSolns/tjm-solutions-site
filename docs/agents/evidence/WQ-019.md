# Evidence — WQ-019

**Item:** Formalize swizzle restriction: write `docs/governance/POL/POL-001-no-swizzle.md` — rule is "no
Docusaurus swizzle; use per-article head block injection pattern instead" (pattern established in
WQ-014). Makes the informal CONTEXT-KERNEL note durable and searchable. Source: retro-all RL-001.

Commit: 156738e

Run-count: 2 (re-read `docs/governance/POL/POL-001-no-swizzle.md` after writing to confirm all 5
required sections — Context, Decision, Rationale, Consequences, Alternatives Rejected — are present per
the `/decide` template; re-read `docs/agents/DECISION-REGISTER.md` after the edit to confirm the DR-001
table row and detail block both exist and the `Ref:` path matches the actual file path)

Invariance-recheck: Cross-checked every factual claim in the POL against its cited source rather than
inventing detail — the WQ-014 stall (BlogPostPage swizzle considered, then replaced by the head-block
pattern) is confirmed by `docs/agents/HANDOFF-LEDGER.md` HL-003 (open item: "WQ-014 ... blocked by
CONTEXT-KERNEL non-negotiable... needs a governance decision") and HL-004 (what actually shipped:
per-article `<head>` block with JSX template-literal JSON-LD, commit 8289b95); the LL-001/LL-004 citations
are confirmed present and matching in `docs/agents/LESSONS-LEARNED.md`; the two prose sources this POL
formalizes (CONTEXT-KERNEL.md "No Docusaurus swizzle — prefer CSS variable overrides" and CLAUDE.md
"Prefer CSS variable overrides in `custom.css` over `yarn swizzle`") were re-read verbatim, not
paraphrased from memory, to confirm the POL's Consequences section correctly states neither needs a
wording change.

Verified-by: verifier (Agent tool, subagent_type: "verifier")

Verifier-tier: haiku (drawn via `python3 .claude/hooks/draw-verifier-tier.py P3 sonnet` — raw_offset=-1
final_offset=-1 resampled=False)

Verifier-verdict: PASS — independent verifier (haiku, fresh context, subagent_type "verifier",
dispatched as a background Agent call for DN-006 transcript corroboration) confirmed commit 156738e is
real via `git show --stat`/`git show`, matching the claimed 3-file change; independently re-derived every
Invariance-recheck claim by reading HL-003 (WQ-014 blocked by the swizzle non-negotiable), HL-004
(WQ-014 shipped via head-block pattern, commit 8289b95, independently confirmed to exist with matching
message), LL-001 and LL-004 in LESSONS-LEARNED.md, CONTEXT-KERNEL.md line 30, and CLAUDE.md line 108 —
all matched the POL's citations exactly; confirmed POL-001's 5 required sections are present and
substantive, and that DR-001's table row + detail block exist in DECISION-REGISTER.md with a correct
`Ref:` path. No VETO issues found.
