# Evidence — WQ-020

**Item:** Formalize remaining CONTEXT-KERNEL quality gates into
`docs/governance/POL/POL-002-deployment-quality-gates.md`: (1) WCAG AA compliance required on all pages
(`npm run wcag` before every deploy); (2) all articles must have canonical links back to original Medium
publication; (3) dark mode overrides use `[data-theme='dark'] .className` in CSS modules — never
`@media (prefers-color-scheme: dark)`. Source: WQ-P4-037 CONTEXT-KERNEL audit.

Commit: 20a683f

Run-count: 2 (wrote `docs/governance/POL/POL-002-deployment-quality-gates.md`, then re-read it back to
confirm all 5 required `/decide` sections — Context, Decision, Rationale, Consequences, Alternatives
Rejected — are present and substantive; re-read `docs/agents/DECISION-REGISTER.md` after the edit to
confirm the new DR-002 table row and detail block both exist, are ordered correctly above DR-001 per the
register's "new entries at TOP" convention, and the `Ref:` path matches the actual file path)

Invariance-recheck: Cross-checked each of the 3 gates POL-002 formalizes against its actual current
source rather than paraphrasing from memory — `docs/agents/CONTEXT-KERNEL.md` Non-Negotiables (lines 25,
28, 31 as of this session: WCAG AA gate, canonical-link requirement, dark-mode `[data-theme='dark']`
rule) and `CLAUDE.md`'s CSS/Dark Mode Rules section (line 56) and Key Constraints section, both re-read
verbatim to confirm POL-002's Consequences section is correct that neither file needs a wording change.
Confirmed the WQ-042 dark-mode contrast bug cited in POL-002's Rationale/Decision sections (a real
dual-theme WCAG finding, not a hypothetical) is accurately described by re-reading its WORK-QUEUE.md
entry: rates page used the banned `@media (prefers-color-scheme: dark)` pattern causing black-on-black
under a manual light/dark mismatch, plus a `--color-gray-500` contrast failure in 6 places — matches
POL-002's citations exactly. Confirmed this closes the WQ-P4-037-sourced gap alongside POL-001 (which
formalized the swizzle rule from the same audit) by re-reading POL-001's own Context section, which
states the swizzle ban was the one rule from that audit already fixed, implying these three were the
remainder — consistent with WQ-020's own item description.

Verified-by: verifier (Agent tool, subagent_type: "verifier")

Verifier-tier: haiku (drawn via `python3 .claude/hooks/draw-verifier-tier.py P3 sonnet` — raw_offset=-1
final_offset=-1 resampled=False)

Verifier-verdict: PASS — independent verifier (haiku, fresh context, subagent_type "verifier",
dispatched as a background Agent call for DN-006 transcript corroboration) confirmed commit 20a683f is
real and touches exactly `docs/agents/DECISION-REGISTER.md` and
`docs/governance/POL/POL-002-deployment-quality-gates.md`; independently confirmed all 5 required
`/decide` sections are present and substantive in POL-002; confirmed DR-002's table row sits above DR-001
and its detail block's `Ref:` path matches the real file location; independently re-read
CONTEXT-KERNEL.md and CLAUDE.md and confirmed all 3 gates are accurately represented with no wording
change needed to either source; independently confirmed the WQ-042 dark-mode contrast bug citation
matches its real WORK-QUEUE.md entry; confirmed the change is docs/governance-only via its own
`git show --stat`. No VETO issues found.
