# Evidence — WQ-025

**Item:** Verify the site is current on live deployment. Deploy freshness and WCAG were satisfied
2026-07-22 (GL-032); the item was narrowed to its last outstanding requirement, a **link-check against
the deployed site**.

Commit: 5c13a7f — the commit that adds this artifact. This is a **verification-only** item with no
code diff of its own; the previous version of this field cited `d8eb180`, which is WQ-050's
assessment commit and unrelated to the link check (corrected after a verifier VETO). The deployed
state under test is
`f45b408` on `main`, live at https://www.tjm.solutions

Run-count: 3 checks at verification time (2026-08-18).
(1) Live-site link check — lychee run against the **deployed** site (not the local build), covering
`/`, `/about`, `/tony`, `/rates`, `/articles`, `/projects` and everything they link to:
`390 Total, 99 Unique, 365 OK, 0 Errors, 25 Excluded`. Zero broken links.
(2) Deploy-freshness spot check — the three articles and two pages shipped this session were fetched
from the live domain and returned 200 with rendered body content, correct canonicals, and the expected
`Person`/`Organization` JSON-LD (recorded in the session transcript for commits ab61544 / 41c1a70).
(3) Local-build link check with the same arguments as a cross-check: `4988 Total, 4560 OK, 0 Errors,
428 Excluded`.

Invariance-recheck: The live and local checks are genuinely different measurements, not the same check
run twice — (1) resolves every URL over the network against GitHub Pages as a visitor would, while (3)
walks the built HTML on disk. They agree on the load-bearing conclusion (zero broken links) while
differing in corpus size, which is the expected signature of two independent routes to the same answer
rather than a duplicated one. The live check is the one that actually satisfies this item, since the
item's whole point is that a passing local build proved nothing about production during the month-long
silent deploy failure (WQ-038).

One material caveat, stated rather than buried: both runs exclude `medium.com` per the WQ-054 fix, so
Medium canonicals are unverified by this check. They are covered by the WQ-027 Medium-parity procedure,
which was exercised this session and found three genuinely missing articles — so that surface is not
unmonitored, it is monitored by a different and more appropriate instrument.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model sonnet) — two passes dispatched
2026-08-18, fresh context each.

Verifier-tier: sonnet (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
tier=sonnet raw_offset=-1 final_offset=-1 resampled=False priority=P2 doer=opus)

Verifier-verdict: PASS on the second pass. The first pass confirmed all four substantive claims but
VETOed because the `Commit:` field cited `d8eb180`, which is WQ-050's assessment commit, unrelated to
the link check; corrected to `5c13a7f` and disclosed as verification-only. The second pass confirmed
that fix and went further than this artifact did on provenance: rather than trusting the `main` tip, it
checked the GitHub Pages **Deployments API**, finding `sha: f45b408d…`, `state: success`, deployed
2026-08-18T06:05:37Z, matching the `Last-Modified: Tue, 18 Aug 2026 06:05:30 GMT` header on a live
fetch of `/tony`. It also flagged a trap worth recording: the legacy `gh-pages` branch is **stale**
from the old `npm run deploy` era and does not reflect the live site, so checking that branch would
have been misleading — the deployment record is the source of truth. It independently crawled all 36
article links from the live `/articles` listing plus 20 `/projects` links (a different corpus from the
lychee run) and found zero broken links.
