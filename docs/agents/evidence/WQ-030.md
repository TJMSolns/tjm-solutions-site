# Evidence — WQ-030

**Item:** Add article "Modeling Distribution on Tracing Ideals: A Novice's View on COVID-19 Vaccine
Management" (Medium 2020-12-06) to the site blog — the oldest Medium article and the last remaining
Medium-parity gap after Parts 12–18. Fetch content from Medium; apply the `<head>` canonical +
JSON-LD pattern; filename `blog/2020-12-06-modeling-distribution-on-tracing-ideals.mdx`; tags per
taxonomy after content review.

Commit: f5dc7fb

Run-count: 4 checks run at verification time (2026-07-22). (1) File present and well-formed —
`blog/2020-12-06-modeling-distribution-on-tracing-ideals.mdx` exists (8043 bytes), carries exactly one
`<!-- truncate -->` marker, frontmatter `title`/`date: 2020-12-06`/`tags: [strategy, architecture]`,
and a `<link rel="canonical" href="https://tmoores.medium.com/modeling-distribution-on-tracing-ideals-a-novices-view-on-covid-19-vaccine-management-39d13158a5a3">`.
(2) `git show --stat f5dc7fb` confirms the commit adds this file (+32 lines) as one of 5 files / 330
insertions. (3) Live at `https://www.tjm.solutions/articles/2020/12/06/modeling-distribution-on-tracing-ideals`
— HTTP 200, and the rendered HTML contains the canonical `<link>` back to the Medium URL (confirms the
BlogPostPage canonical injection fired). (4) `npm run build` with `onBrokenLinks: 'throw'` passed
earlier this session for the commit (the deploy that produced the live page), confirming the new MDX
integrates with no broken internal links.

Invariance-recheck: Re-verified the Medium-parity invariant (the reason this item exists) rather than
only the single file. Diffed the full Medium article set — the sitemap loc list at
`https://tmoores.medium.com/sitemap/sitemap.xml` — against every `rel="canonical"` Medium URL across
`blog/*.mdx`: `comm -23 medium.txt site_now.txt` returns empty, i.e. the site is now a superset of the
Medium sitemap with zero remaining gaps (before this session the same diff returned 3 URLs, of which
this article was one). Also confirmed the article's canonical URL appears in exactly one blog file
(`grep -rl` → the one expected file, no accidental duplicate). Note for the independent verifier: the
canonical for THIS article was present in the Medium *sitemap*, so a sitemap-only diff suffices to
re-check it; the two FP-series gaps closed alongside it in the same commit are covered by their own
rows, not this one.

Verified-by: doer self-checks above run 2026-07-22; independent verification pending (see Verifier-verdict).

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=0 final_offset=0 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — independent verifier not yet dispatched. No PASS written until a real
`subagent_type: "verifier"` Agent call at the drawn tier (opus) returns it.
