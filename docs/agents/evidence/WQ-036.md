# Evidence — WQ-036

**Item:** Add the remaining 13 offer detail pages (CS-1/5/6/7/8/9, DT-1/4/6, EE-2/3/6/7) to complete
the 22-offer packaged-offers catalog on the site, using the existing `OfferPage` spec-sheet component.

Commit: 28fde1f

Run-count: 2 (`npm run build` with `onBrokenLinks: 'throw'` — passed, confirming all 13 new routes
build and all internal links resolve; `npm run typecheck` — no new errors, only the 6 pre-existing
`Layout` prop-type errors already present in `OfferPage/index.tsx` and 5 other pre-existing pages
before this session)

Invariance-recheck: Confirmed all 13 expected files present under `build/services/*.html`
(agentic-commerce-transition-roadmap, ai-adoption-roadmap, b2b-commerce-modernization-strategy,
commerce-platform-assessment, commerce-platform-selection, commerce-technology-due-diligence,
commerce-vendor-si-gtm-advisory, customer-engagement-analytics-review, data-readiness-for-ai,
digital-experience-platform-assessment, merchandising-business-tools-assessment,
personalization-recommendations-strategy, technical-roadmap-business-alignment), plus the original 9
still present (22 total). Ran `npm run wcag` (the 4 fixed pages it samples — home/about/rates/articles
— all "No issues found"), then separately sampled 6 of the 13 new `/services/*` pages directly with
`pa11y --standard WCAG2AA` in default (light) theme — all "No issues found" — and re-ran 4 of those
same pages with the localStorage `theme=dark` injection pattern (HL-007 pattern) to force dark theme
before navigation — all "No issues found" in dark mode too. Grepped all 13 new files for the banned
employer/client names (RETISIO, ATG, Oracle, Professional Access, Mirakl) and for a `credibility` field
— zero matches on both, matching the precedent that all 9 existing pages now omit the credibility
section entirely (WQ-042). Also confirmed via `git status --short` in this repo that only the 13
intended files were staged/committed (no incidental changes to HomepageFeatures, index.tsx, or the 9
existing offer pages).

Verified-by: verifier (Agent tool, subagent_type: "verifier")

Verifier-tier: haiku (drawn via `python3 .claude/hooks/draw-verifier-tier.py P3 sonnet` — raw_offset=-1 final_offset=-1 resampled=False)

Verifier-verdict: PASS — independent verifier (haiku, fresh context, subagent_type "verifier") re-checked
commit 28fde1f, re-ran the build, independently re-grepped for banned names/credibility fields, and
confirmed the OfferPage pattern and category/categoryHref consistency across all 13 files. No VETO
issues found.
