# Evidence — WQ-037

**Item:** Redesign the homepage offer list from stacked full-sentence links to compact wrapped chips
(mono code + short name, colored by category), and expand each category's list from the 3 curated
offers to the full 22-offer catalog (CS 9, DT 6, EE 7) now that WQ-036 completed all remaining offer
pages.

Commit: 9dbf3f1

Run-count: 3 (`npm run typecheck` — same 6 pre-existing `Layout` prop-type errors as WQ-036's baseline,
no new errors from the changed files; `npm run build` with `onBrokenLinks: 'throw'` — passed, confirming
all 22 offer links from the expanded chip lists resolve; `npx pa11y --standard WCAG2AA` against the
homepage in light theme — "No issues found")

Invariance-recheck: Ran a second, independent pa11y pass in dark theme using the HL-007
localStorage-injection pattern (`page.evaluateOnNewDocument(() => localStorage.setItem('theme', 'dark'))`
before navigation, confirmed via `document.documentElement.getAttribute('data-theme') === 'dark'`) —
"0 issues" against the homepage. Took full-page Puppeteer screenshots in dark theme, light theme, and a
390×844 mobile viewport and visually confirmed: (1) all 9/6/7 offers now render as wrapped chip rows
under each category (CS/DT/EE), matching the "9 OFFERS"/"6 OFFERS"/"7 OFFERS" counts already computed
from `offers.length`; (2) each chip's border and mono code text pick up the category color
(`--cat-cs`/`--cat-dt`/`--cat-ee`) correctly in both themes; (3) chips wrap cleanly at desktop width and
stack to one-per-row on mobile (longer labels wrap to a second line inside their own chip — readable, no
overflow or clipping); (4) no wall-of-text regression — the category header rows (icon, code, name,
one-line description) were left untouched per the WQ-037 spec, only the offer-list markup/CSS changed.
Confirmed via `git diff --stat` that only `src/components/HomepageFeatures/index.tsx` and
`styles.module.css` changed — no incidental edits to other pages or components.

Verified-by: verifier (Agent tool, subagent_type: "verifier")

Verifier-tier: haiku (drawn via `python3 .claude/hooks/draw-verifier-tier.py P3 sonnet` — raw_offset=-1
final_offset=-1 resampled=False)

Verifier-verdict: PASS — independent verifier (haiku, fresh context, subagent_type "verifier",
dispatched as a background Agent call for DN-006 transcript corroboration) re-checked commit 9dbf3f1,
confirmed all 22 offers present and correctly split (CS 9 / DT 6 / EE 7), independently re-ran
`npm run typecheck` and `npm run build` (onBrokenLinks: throw), independently re-ran pa11y WCAG2AA in
both light and dark theme (own Puppeteer dark-theme injection, not the doer's script), took its own
light/dark/mobile screenshots, and confirmed via `git diff --stat` that only the 2 intended files
changed. No VETO issues found.
