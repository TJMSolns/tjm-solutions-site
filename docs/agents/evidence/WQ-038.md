# Evidence — WQ-038

**Item:** Migrate the GitHub Pages deploy from the legacy branch-push mechanism (`docusaurus deploy` →
`gh-pages`) to an Actions-based workflow (`upload-pages-artifact` / `deploy-pages`); the legacy build
had been silently failing since 2026-05-16. Also fix a link-check CI race (link-check ran concurrently
with deploy and 404'd on the live site mid-deploy).

Commit: 2e32c7c (migration), 8c225ff (follow-up)

Run-count: 2 checks at verification time (2026-07-22). (1) `git show --stat 2e32c7c` — real, adds the
Actions deploy job to `.github/workflows/ci.yml` (+26 lines). (2) `grep` of the current
`.github/workflows/ci.yml` — confirms it uses `actions/upload-pages-artifact@v3` (line 37) and
`actions/deploy-pages@v4` (line 53), i.e. the official GitHub Pages Actions path, not a branch push.

Invariance-recheck: Read the current `.github/workflows/ci.yml` directly rather than trusting the
commit message. The deploy is Actions-based: an `upload-pages-artifact@v3` step publishes the built
`build/` dir as a Pages artifact and a separate `deploy-pages@v4` job deploys it — no `docusaurus deploy`
/ `gh-pages` branch push remains in the workflow. This session is live evidence the pipeline works end
to end: today's publish of 3 articles + 2 résumés (commit f5dc7fb) reached production through it and all
6 URLs verified 200. The link-check race fix is corroborated by the ordering in the same workflow
(link-check gated relative to deploy rather than concurrent). Independent verifier should confirm against
the current `ci.yml`.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model sonnet) — dispatched 2026-07-22.

Verifier-tier: sonnet (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=-1 final_offset=-1 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — awaiting the independent sonnet verifier's result.
