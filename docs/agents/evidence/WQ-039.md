# Evidence — WQ-039

**Item:** Site-wide visual identity redesign (2026-07-05/06, Tony direction): terminal-profile-inspired
dark-primary palette (deep red/blue/green mapped to CS/DT/EE); Bricolage Grotesque + Public Sans + Space
Mono type system; logo recolored #C00000→#770000; self-hosted Phosphor icons for the 3 category marks;
homepage rebuilt as a cover-sheet hero + CS/DT/EE spec index (replacing the stock 3-card image grid);
all 9 offer pages rebuilt as spec sheets; stock `banner.jpg` removed site-wide in favor of the logo for
og:image; **employer-name credibility claims removed from 6 offer pages and a 7th genericized**
(legal-exposure concern, Tony 2026-07-06, offer pages only — About page bio untouched); full WCAG AA
pass in both themes after fixing a gray-scale design-token bug that had silently broken dark-mode
contrast.

Commit: 2e55512

Run-count: 2 checks at verification time (2026-07-22). (1) `git show --stat 2e55512` — real, 27 files /
488 insertions / 451 deletions, the broad footprint a site-wide redesign implies. (2) `npm run build`
with `onBrokenLinks: 'throw'` — passed.

Invariance-recheck: Re-checked current state directly. The type system + palette + logo recolor live in
`src/css/custom.css` (grep confirms Bricolage/Public Sans/Space Mono and the `#770000` logo color are
present there now). The build is clean. **On the credibility removal (the part entangled with the open
WQ-045 policy question):** removing employer-name credibility claims from the offer pages was an
explicit Tony direction (2026-07-06, legal-exposure), i.e. it is the *intended* deliverable of this
item, not a regression — current state (`grep -ril credibility src/pages/services/` = 1 of 22) is
consistent with the removal having happened. The separate question of whether that removal leaves a
PDR-008 listability gap is tracked as WQ-045 and is **not** a defect in WQ-039's execution; Tony
reviewed the redesigned pages and signed off the offer-page work 2026-07-22 (HL-065). Independent
verifier should confirm the redesign deliverables (type/palette/logo in custom.css, spec-sheet homepage
+ offer pages, banner.jpg no longer the og image, clean build) and treat the credibility removal as
intended, not as grounds for VETO.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model sonnet) — dispatched 2026-07-22.

Verifier-tier: sonnet (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=-1 final_offset=-1 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — awaiting the independent sonnet verifier's result.
