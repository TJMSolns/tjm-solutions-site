# Evidence — WQ-040

**Item:** Fix the hero grid-texture mask fading text content (the mask was applied to the whole hero
box, washing out text past ~40% height) by moving the grid+mask to a `::before` pseudo-element behind
the text; extend the Phosphor/Glyph icon system (clock/check-square/users/calendar) to offer-page
section headings and the CTA button; trim homepage category descriptions to one sentence each.

Commit: cd6f88c

Run-count: 1 check at verification time (2026-07-22): `npm run build` with `onBrokenLinks: 'throw'` —
passed (`[SUCCESS] Generated static files`).

Invariance-recheck: `git show --stat cd6f88c` confirms the commit touches exactly the components this
item names — `HomepageFeatures/index.tsx`, `OfferPage/index.tsx` + its `styles.module.css`,
`ArticlesList/styles.module.css`, and adds `GlyphIcon/index.tsx` — consistent with (a) moving the
grid-texture mask to a pseudo-element in the shared hero/offer styles and (b) extending the icon system.
The current tree builds cleanly. Independent verifier should confirm from the diff that the grid/mask
moved to a `::before` (or equivalent decorative layer) rather than sitting on the text-bearing box, and
that the GlyphIcon component exists and is referenced by OfferPage. This is a visual/CSS change; the
re-derivable proof is the diff + a clean build (a broken selector or missing import would fail the
build).

Verified-by: verifier (Agent tool, subagent_type: "verifier", model opus) — dispatched 2026-07-22.

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=0 final_offset=0 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — awaiting the independent opus verifier's result.
