# Evidence — WQ-041

**Item:** Fix retainer-card density on the rates page — switch the engagement grid from `auto-fit`
(which forced all 4 cards into one equal-width row and squeezed the retainer's 3 pricing tiers) to a
fixed 2×2. Add a missing header/hero to the projects page (which previously went straight into the card
grid). Migrate the projects page's hardcoded `#c00000` colors to theme tokens.

Commit: 41adef9

Run-count: 1 check at verification time (2026-07-22): `npm run build` with `onBrokenLinks: 'throw'` —
passed.

Invariance-recheck: `git show --stat 41adef9` confirms the commit touches exactly `rates.module.css`
(retainer grid), `projects.tsx` (+7 lines — the added header/hero), and `projects.module.css` (+57 lines
— header styles + token migration), matching all three sub-claims. Independent verifier should re-read
the current files to confirm: (a) the rates engagement grid is a fixed 2-column layout rather than
`auto-fit`; (b) `projects.tsx` now renders a header/hero block above the card grid; (c) `projects.tsx` /
`projects.module.css` reference CSS variables (e.g. `var(--cat-*)` / `--ifm-*`) rather than a literal
`#c00000`. A clean build confirms no broken selectors/imports resulted.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model opus) — dispatched 2026-07-22.

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=0 final_offset=0 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — awaiting the independent opus verifier's result.
