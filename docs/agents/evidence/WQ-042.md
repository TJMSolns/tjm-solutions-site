# Evidence — WQ-042

**Item:** Fix two real WCAG contrast bugs caught by dual-theme testing: (1) rates page
`.disclaimer`/`.ctaSection` used the banned `@media (prefers-color-scheme: dark)` pattern instead of
`[data-theme='dark']`, causing black-on-black when the OS is dark but the site is toggled light;
(2) `--color-gray-500` used directly as text color failed light-mode contrast (3.4–3.74:1, needs 4.5:1)
in 6 places. Also: remove the credibility section from the remaining 4/9 offer pages that had never
named a company (structural consistency with the 5 removed for legal reasons in WQ-039), and add the
grid-texture hero treatment to offer pages.

Commit: 99feb28

Run-count: 3 checks at verification time (2026-07-22). (1) `git show --stat 99feb28` — real, 9 files /
88 insertions / 74 deletions. (2) `grep -c "prefers-color-scheme" src/pages/rates.module.css` — returns
**0**, confirming the banned pattern was removed (contrast bug 1 fixed). (3) `npm run build` with
`onBrokenLinks: 'throw'` — passed.

Invariance-recheck: Re-checked current state directly rather than trusting the commit message. Bug 1:
the banned `@media (prefers-color-scheme: dark)` is gone from `rates.module.css` (count 0) — the file now
uses the `[data-theme='dark']` pattern the CONTEXT-KERNEL/POL-002 rule requires. Bug 2 (gray-500 direct
text color) and the offer-page changes are in the same commit's diff. **On the credibility removal (the
part entangled with WQ-045):** removing the credibility section from the remaining 4/9 offer pages was
this item's *stated deliverable* (structural consistency after WQ-039's legal-exposure removals), so
current state (`grep -ril credibility src/pages/services/` = 1 of 22) confirms the work was done, not
that something regressed. Whether the resulting site-wide absence satisfies PDR-008 is the separate
open question tracked as WQ-045 — not a defect in WQ-042. Independent verifier should confirm the two
contrast fixes (no `prefers-color-scheme` in rates; gray-500 no longer used as raw text color) and treat
the credibility removal as intended. A dual-theme WCAG re-check is welcome; note `npm run wcag` passed
clean on its 4 covered pages this session.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model opus) — dispatched 2026-07-22.

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=1 final_offset=1 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — awaiting the independent opus verifier's result.
