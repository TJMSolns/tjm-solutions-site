# Evidence — WQ-043

**Item:** Standardize all 7 hero banners (home / about / rates / articles / projects / mdslides /
offer-pages) to one identical padding/border spec after an audit found border-bottom on 6/7, padding
varying 3 ways, and content-volume inconsistency. Trim mdslides' subtitle to one line; remove the
description from offer-page heroes (kept for meta/OG only).

Commit: 0fbf049

Run-count: 1 check at verification time (2026-07-22): `npm run build` with `onBrokenLinks: 'throw'` —
passed.

Invariance-recheck: `git show --stat 0fbf049` confirms the commit spans 9 files with a small,
consistency-shaped diff (7 insertions / 17 deletions — net removal, consistent with deleting redundant
per-page hero border/padding overrides and hero descriptions rather than adding new markup). That
net-negative, spread-across-hero-pages shape matches "converge 7 heroes on one spec + drop two bits of
content." Independent verifier should spot-check that the hero style is now sourced from one shared spec
(the per-page hero CSS overrides removed) and that offer-page heroes no longer render a description
paragraph while retaining it in meta/OG. A clean build confirms no dangling references.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model sonnet) — dispatched 2026-07-22.

Verifier-tier: sonnet (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=-1 final_offset=-1 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — awaiting the independent sonnet verifier's result.
