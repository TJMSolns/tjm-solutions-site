# Evidence — WQ-044

**Item:** Gitignore `.claude/hooks/__pycache__/` — untracked Python bytecode cache from hook execution,
found during a `/handoff` git-audit gate.

Commit: fead2e7

Run-count: 2 checks at verification time (2026-07-22). (1) `git show --stat fead2e7` — real, `.gitignore`
+1 line, matching the one-line-change claim. (2) `grep -n pycache .gitignore` — `__pycache__/` present
at line 25.

Invariance-recheck: Read the current `.gitignore` directly — line 25 is `__pycache__/`, a repo-wide
ignore that covers `.claude/hooks/__pycache__/` and any other Python cache dir. Confirmed via
`git status --short` earlier this session that no `__pycache__` path appears as tracked or untracked
noise. This is a single-line config change with no runtime behavior; the re-derivation is reading the
file, which is definitive.

Verified-by: verifier (Agent tool, subagent_type: "verifier", model opus) — dispatched 2026-07-22.

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P2 opus` —
raw_offset=0 final_offset=0 resampled=False priority=P2 doer=opus)

Verifier-verdict: PENDING — awaiting the independent opus verifier's result.
