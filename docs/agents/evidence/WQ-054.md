# Evidence — WQ-054

**Item:** P1 BUG — CI `Link check` job (lychee) fails on every run. All reported errors are `[403]`
responses from `tmoores.medium.com`, not real broken links. Fix: add `--exclude 'medium\.com'` to the
lychee args in `.github/workflows/ci.yml`, matching the existing exclusions for `linkedin.com`,
`twitter.com` and `x.com`, which are excluded for the same bot-blocking reason.

Commit: d8bd4e4 (the fix) and 1677829 (count correction after verifier VETO)

Run-count: 5 checks at verification time (2026-08-18).
(1) `gh run view 32103981632 --log-failed` — 122 `[403]` error lines, every one against
`tmoores.medium.com`: 85 occurrences of the bare site-wide footer link plus 37 canonical occurrences,
spanning 31 unique URLs. **Correction 2026-08-18:** the first version of this artifact said 141, a
figure that was asserted rather than computed and that matches no counting convention in the log; the
verifier caught it, and the same wrong number had been committed into **two** tracked files: the
comment in `.github/workflows/ci.yml` **and** the WQ-054 row in `docs/agents/WORK-QUEUE.md`. The first
correction pass fixed only `ci.yml` and this artifact, missing the WORK-QUEUE row, and a second verifier
VETO caught that omission. Recomputed
directly: `grep -c '\* \[403\]'` = 122, `grep -oE '\[403\] <https://tmoores\.medium\.com/>' | wc -l`
= 85, canonical form = 37, and 85 + 37 = 122. All three files — this artifact, `ci.yml`, and the WQ-054 row in `WORK-QUEUE.md` — now state 122. The phrase "one per article canonical" was also loose and has been replaced with the exact figures: 37 canonical occurrences across 30 unique canonical URLs.
(2) `gh run view 32011196437 --log-failed` — the scheduled run from 2026-08-17, before any of that
day's commits, failed identically (81 footer + one per canonical), establishing the failure as
systemic and pre-existing rather than a regression.
(3) User-agent probe — `curl` with default UA, with a Chrome browser UA, and with `-I` (HEAD, which is
lychee's first request method) against both `https://tmoores.medium.com/` and an article URL: all six
combinations returned 403. This rules out the cheaper fix of setting `--user-agent` and is why
exclusion is the correct remedy rather than a workaround.
(4) `python3 -c "yaml.safe_load(...)"` on the edited `ci.yml` — parses, and the resolved `args` string
contains no `#` character. This check exists because the first version of the edit put explanatory
comments *inside* the `args: >-` folded block scalar, where YAML would have folded them into the
argument string and passed them to lychee as arguments. The comment was moved above the step.
(5) Direct before/after execution of lychee itself via `docker run lycheeverse/lychee:latest` against
the real `build/` output, using the exact argument list from `ci.yml`:
  - without `--exclude 'medium\.com'`: `4560 OK, 122 Errors, 306 Excluded`
  - with `--exclude 'medium\.com'`:    `4560 OK, 0 Errors, 428 Excluded`
Same corpus, same flags, single variable changed. The 122 errors move into the excluded bucket
(306 + 122 = 428) and the OK count is unchanged at 4560, showing the exclusion suppresses exactly the
Medium URLs and nothing else.

Invariance-recheck: Re-derived the conclusion by a different route than the CI log that motivated the
item. Rather than trusting the job's own error report, the checker was run locally against the build
artifact with the exclusion toggled, and the OK count held constant at 4560 across both runs while
errors went 122 -> 0 and exclusions went 306 -> 428. That arithmetic is the load-bearing part: it
demonstrates the fix removes only the Medium URLs from checking and does not mask any other failure,
which a simple "job now passes" observation would not establish. Separately, the claim that a
user-agent fix was unavailable was tested directly (6 probes) rather than assumed from the 403s.
Residual risk, stated honestly: Medium links are now unchecked by CI, so a rotted Medium canonical
would not be caught here. That surface is covered by the WQ-027 Medium-parity check, which diffs the
Medium index against `blog/*.mdx` canonicals — a check this session exercised and used to find the
three missing articles.

Verified-by: PENDING

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P1 opus` —
tier=opus raw_offset=-1 final_offset=0 resampled=True priority=P1 doer=opus)

Verifier-verdict: PENDING
