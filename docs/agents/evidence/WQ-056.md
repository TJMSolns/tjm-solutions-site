# Evidence — WQ-056

**Item:** Fix live WCAG2AA contrast failures in syntax-highlighted code, both themes. WQ-056's row
named 4 failing light-theme prism tokens (233 errors across 7 pages) and 1 failing dark-theme token
(5 errors across 3 pages), root cause undiagnosed.

**Root cause:** `docusaurus.config.ts` used `prism-react-renderer`'s stock `themes.github` (light) and
`themes.dracula` (dark) unmodified. Both ship token colors that were never checked against this site's
actual code-block backgrounds (`#f6f8fa` light, `#282a36` dark) for 4.5:1 contrast.

**Fix:** `src/prismTheme.ts` (new file) derives `lightCodeTheme`/`darkCodeTheme` from the stock themes,
overriding only the failing token colors' hex/rgb values (hue preserved, lightness adjusted), computed
via the standard WCAG relative-luminance formula. `docusaurus.config.ts` now imports these instead of
`prismThemes.github`/`prismThemes.dracula` directly. No swizzle (POL-001 compliant, config-level change
only).

**Scope correction found during the fix:** a 5th light-theme token, `function`/`deleted`/`tag`
(`#d73a49`, 4.30:1), was not in WQ-056's original 4-token diagnosis. Found while computing fixes for the
other four, by checking every token in `themes.github.styles` against the background, not just the ones
already named. Fixed alongside the other four; see `src/prismTheme.ts`'s table for all 6 before/after
ratios (4.52–4.59 after, all with a small margin above 4.5, not exactly at the line).

Commit: (pending, see branch `fix/2026-08-18-wcag-syntax-contrast`)

Run-count: 3 independent checks. (1) `npm run typecheck` — 0 errors. (2) `npm run build` — succeeds,
`onBrokenLinks: 'throw'` does not fire. (3) `npx pa11y` re-run against all 8 pages WQ-056 originally
named (`/articles/2026/04/21/protocol-stack-agent-native-commerce`, `/mdslides/docs/themes`,
`/articles/2026/04/21/capability-surfaces`, `/mdslides/docs/writing-slides`,
`/mdslides/docs/configuration`, `/mdslides/docs/speaker-view`, `/mdslides/docs/navigation`), plus a
dark-theme recheck on the first. Grepped each light-theme result for `token`/`pre`/`code` selectors:
zero matches, confirming no remaining syntax-highlight contrast errors in either theme.

Invariance-recheck: light theme is not the site's default (dark is: `colorMode.defaultMode: 'dark'`),
so pa11y's default run only exercises dark. Re-checked light explicitly via the same forced-theme-toggle
`actions` config WQ-051/WQ-052 established (click the navbar theme button, wait for
`[data-theme='light']`).

**Self-correction:** the first light-theme re-check still showed errors on these pages, and was briefly
attributed to a separate bug (filed as WQ-063). That was wrong, and the fault was the check, not the
site. The `actions` config waited only for the `[data-theme='light']` attribute to appear, not for
styles to finish recalculating after the toggle, producing false positives (nonsensical recommendations
like "change text colour to #fff" on elements that were never dark). Adding an explicit `--wait 2000`
after the toggle action (the same fixed-wait fix HL-067 already recorded for this exact class of pa11y
timing issue) resolved it: all 8 pages, both themes, genuinely clean, zero errors. WQ-063 is being
retracted for the same reason on the branch where it was filed.

Verified-by: (pending independent verifier per DN-002)

Verifier-tier: (pending)

Verifier-verdict: PENDING
